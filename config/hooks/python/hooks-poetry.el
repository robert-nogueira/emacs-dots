;;; hooks-poetry.el --- Poetry integration for python-mode

;;; Commentary:
;; LSP startup and venv detection for python-mode is handled in
;; python.el's lsp-pyright hook.

;;; Code:

(provide 'hooks-poetry)
;;; hooks-poetry.el ends here
